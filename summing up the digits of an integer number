#include<stdio.h>
int main()
{
	int n,sum=0,m;
	printf("enter a number:");
	scanf("%d",&n);
	while(n>0)
	{
		n=n%10;
		sum=sum+m;
		n=n/10;
	}
	printf("sum is = %d",sum);
	return 0;
}

OUTPUT:
enter a number:8
sum is = 8
